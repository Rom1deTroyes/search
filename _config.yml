# Setup
title:        Rom1deTroyes/search
baseurl:      "/search"
repository:    Rom1deTroyes/search

# Scrolltop color
scrolltop_color: "#664266"

# Collections
collections:
    issues:
      output: false
      permalink: /:collection/:title/

# Defaults
defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

#Others
markdown: kramdown

plugins:
  - github-pages
